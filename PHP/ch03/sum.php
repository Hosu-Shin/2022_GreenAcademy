<?php
    $n1 = 10;
    $n2 = 20;
    $sum = $n1 + $n2;

    print "<div>" . $sum . "</div>";

    print "<br>";

    $sub = 12.3 - 42.72;
    print $sub;

    /* = 뒤의 값들을 리터럴(Literal)이라고 부른다
    프로그램에서 상수와 리터럴은 다른다 
    상수는 대입된 값이 변하지 않는다 */
    
    // 리터럴을 값을 변경할 수가 없어서 변수자리에 올 수 없다
?>